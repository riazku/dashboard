[phases.setup]
nixPkgs = ["php82", "composer", "mysql", "nodejs"]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm install && npm run build"
]

[phases.start]
cmds = ["php artisan serve --host=0.0.0.0 --port=$PORT"]
